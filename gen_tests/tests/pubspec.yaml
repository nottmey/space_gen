name: tests
description: Tests for the generated gen_tests directory.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  preserve_unknown_enums:
    path: ../preserve_unknown_enums
  types:
    path: ../types

dev_dependencies:
  http: ^1.4.0
  lints: ^6.0.0
  test: ^1.25.6
